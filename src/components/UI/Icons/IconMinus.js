import './index.css';
import iconMinus from '../../../img/minus.png';

const IconMinus = props => {
  return (
    <img src={iconMinus} className="icon-minus" alt="default" />
  );
};

export default IconMinus;
