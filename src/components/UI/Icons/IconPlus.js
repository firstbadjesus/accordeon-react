import './index.css';
import iconPlus from '../../../img/plus.png';

const IconPlus = props => {
  return (
    <img src={iconPlus} className="icon-plus" alt="default" />
  );
};

export default IconPlus;